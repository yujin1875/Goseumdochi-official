spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://34.64.219.194:3306/db_midas?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: midas
    password: midas
    hikari:
      connection-test-query: select 1 from dual

    cloud:
      gcp:
        storage:
          credentials:
            location: classpath:midas-vm-instance-03c538b381b1.json
      project-id: midas-vm-instance
      bucket: studentprofile

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        enable_lazy_load_no_trans: true
    hibernate:
      ddl-auto: update